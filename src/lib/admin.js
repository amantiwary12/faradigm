// The admin lives at a path only the owner knows. It is not linked from the site, not in the menu,
// not in search, and every admin page sends noindex.
export const ADMIN_BASE = '/admin/faradigm/aar';
